{
  "compilerOptions": {
    "baseUrl": ".", // baseUrl을 명시하지 않으면 paths 사용할 수 없음
    "target": "es6", // 어떤 버전의 JS로 변환할 지
    "module": "esnext", // 모듈 시스템 지정 - ESNext는 트리 쉐이킹 등 웹팩 최적화 기능을 더 잘 활용 가능
    "strict": true, // 엄격한 타입 체크를 켜서 실수를 줄임
    "skipLibCheck": true, //라이브러리 파일의 타입 검사를 건너뛰어 빌드 속도를 높임
    "moduleResolution": "node", // 모듈이 로컬 파일 시스템에 없으면, node_modules 디렉토리에서 모듈을 찾음
    // "jsx": "react", // JSX를 React 문법으로 변환
    "jsx": "react-jsx",
    "esModuleInterop": true, // React를 ESM으로 import할 수 있게 함
    "allowJs": true, // web-vitals를 쓰기위함 *.d.ts로 타입을 정의해주거나 js를 허용하는 식으로 맞춰야 한다.
    "checkJs": false,
    "paths": {
      "@/*": ["src/*"]
    }
  },
  // "include": ["src/**/*.ts", "src/**/*.tsx"], // 컴파일러가 처리할 파일을 지정 
  "include": ["src"],
  "exclude": ["node_modules", "dist"] // 컴파일러가 무시할 파일을 지정 현재 파일에서는 외부 모듈과 빌드 결과물을 제외한다.
}